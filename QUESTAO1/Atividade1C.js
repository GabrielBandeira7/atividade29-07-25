const prompt = require("prompt-sync")();

let numero1 = Number(prompt("Digite um número qualquer no espaço: "));
let numero2 = Number(prompt("Digite outro número: "));

console.log("Soma:", numero1 + numero2);
